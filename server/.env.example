# Server
PORT=4000
CLIENT_URL=http://localhost:5173
SESSION_SECRET=dev-session
MONGO_URI=mongodb://127.0.0.1:27017/bizpilot

# Modes
OFFLINE_MODE=0
LLM_PROVIDER=openrouter  # one of: openai | groq | openrouter

# OpenAI (if LLM_PROVIDER=openai)
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini

# Groq (if LLM_PROVIDER=groq)
# GROQ_API_KEY=...
# GROQ_MODEL=llama-3.1-8b-instant

# OpenRouter (if LLM_PROVIDER=openrouter)
# Sign up at https://openrouter.ai
OPENROUTER_API_KEY=
OPENROUTER_MODEL=mistralai/mistral-7b-instruct:free

# Web research (optional)
# Get key at https://tavily.com
# Enables citations under assistant messages
TAVILY_API_KEY=

# Google OAuth (optional)
# Create credentials; set authorized redirect to:
#   http://localhost:4000/api/auth/google/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=